import React from "react";
import {} from "./MainPageAppElements";

const MainPage = ({ content }) => {
  return (
    <>
      <div style={{}}>{content.firsttext}</div>
    </>
  );
};

export default MainPage;
