//file for business purposes.
//routes functionality and logics shall be here
//const { blogpostDb } = require('../db') TODO require some db

const createFindoc = async (data) => {
  console.log("creating findoc with data: " + data);
  //   try {
  //     return await blogpostDb(user, content);
  //   } catch (e) {
  //     throw new Error(e.message);
  //   } some logic here
};

module.exports = {
  createFindoc,
};
